import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    marginBottom: 32,
  },
  text: {
    fontSize: 16,
    color: "#5A5A5A",
    lineHeight: 24,
  },
  bold: {
    fontWeight: "bold",
    color: "#2C3E50",
  },
});
